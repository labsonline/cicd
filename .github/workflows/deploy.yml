name: Deploy

on:
  workflow_call: {}

jobs:
  # TODO: Deploy to kind cluster
  kind:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Create kind cluster
        uses: helm/kind-action@v1
